Running the Agent In Windows:
1. Set env variables in .env file e.g. `set KEY=abc`
2. Run `python agent.py`

Running Agent as Local Server:
1. Create langgraph.json if it does not exist
2. Add .env file populated with required env vars
3. Execute `langgraph dev`
